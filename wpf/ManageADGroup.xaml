<Window 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="16"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowStyle="None"
        AllowsTransparency="True"
        Title="AD Group Manager" MinHeight="500" MinWidth="450" Height="500" Width="445">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
<!-- INSERT RESOURCES IN CODE HERE -->
                <ResourceDictionary>
                    <ControlTemplate x:Key="MessageDialog" TargetType="materialDesign:DialogHost">
                        <DataTemplate>
                            <StackPanel Margin="50">
                                <TextBlock Text="{Binding Title}" />
                                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
                                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                        materialDesign:ShadowAssist.ShadowDepth="Depth3"
                                        IsCancel="True"
                                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                                        HorizontalAlignment="Center"
                                        Margin="5">
                                    OK
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </ControlTemplate>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>            
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10,10,10,10">
        <StackPanel>
            <Label Name="lblGroup" Content="Active Directory Group Members" HorizontalAlignment="Left" Margin="10,10,10,0" VerticalAlignment="Top" Width="Auto"/>
            <DataGrid Name="dgUsers" HorizontalAlignment="Left" Margin="20,10,10,10" VerticalAlignment="Top" IsReadOnly="true"/>
            <Button Name="btnRemoveUser" Content="Remove" HorizontalAlignment="Left" Margin="20,10,10,10" VerticalAlignment="Top" Height="30" IsEnabled="false"/>
            <Label Name="lblSearch" Content="Search by email address" HorizontalAlignment="Left" Margin="10,10,10,0" VerticalAlignment="Top" Width="Auto"/>
            <TextBox Name="tbSearch" HorizontalAlignment="Left" Height="28" Margin="10,10,10,0" TextWrapping="Wrap" VerticalAlignment="Top" VerticalContentAlignment="Center" Padding="5,0,0,0" Width="250" ToolTip="Search by email address" AutomationProperties.HelpText="Search by email address"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,0,10,0" VerticalAlignment="Top">
                <Button Name="btnSearch" Content="Search" HorizontalAlignment="Left" Margin="10,10,10,0" VerticalAlignment="Top" Height="30" />
                <Button Name="btnAddUser" Content="Add" HorizontalAlignment="Left" Margin="20,10,0,10" VerticalAlignment="Top" Height="30" IsEnabled="false"/>
            </StackPanel>
            <Label Name="lblHint" Content="" HorizontalAlignment="Left" Margin="10,10,10,0" VerticalAlignment="Top" Width="Auto" />
        </StackPanel>
    </Grid>
</Window>